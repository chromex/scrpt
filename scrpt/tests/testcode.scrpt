func main(arg1, arg2)
{
	// Values
	strings = {"", "simple", "new\nline", "\"quote\" of doom", "\ttab\t\tmore"};
	numbers = {0, 1, 0.1234, 19203.1};
	booleans = {true, false};

	// Basic expressions
	val = numbers[2];
	slice = numbers[1:];
	a = !b;
	t = a != b;
	t = a < b;
	t = a > b;
	t = a <= b;
	t = a >= b;

	// Math
	v = 1 + 3.2 - 2.2 * 3.0 / 0.0;
	v += 3;
	v -= val;
	v *= 5;
	v /= 2;

	// Control flow
	test(1, 0, "foo");
	if (true == false)
	{
		print("truth");
	}
	switch(arg1)
	{
	case "yes": print("y"); break;
	default: print("n");
	}
	while(false)
	{
	}
	for(i = 0; i < 10; i += 1)
	{
	}
	do
	{
	} while(false);
}

func test(a, b, c)
{
	return true;
}